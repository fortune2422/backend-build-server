#!/usr/bin/env sh
if [ -x "./gradlew" ] ; then
  exec "./gradlew" "$@"
fi
echo "Gradle wrapper not found. Please open project in Android Studio and let it generate the wrapper, or install Gradle 8.5+ locally."
gradle "$@"
